model:
  name: "RandomForestClassifier"
  params:
    n_estimators: 100
    max_depth: null
    min_samples_split: 2
    min_samples_leaf: 1
    random_state: 42

data:
  test_size: 0.2
  random_state: 42
